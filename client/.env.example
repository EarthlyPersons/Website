# Optional: production API origin (e.g. https://api.healingpractice.co.uk)
# Leave unset in local dev to use the Vite proxy at /api → http://localhost:8787
VITE_API_URL=

# Optional: override the dev proxy target (defaults to http://localhost:8787)
# VITE_DEV_API_PROXY_TARGET=http://localhost:8787
